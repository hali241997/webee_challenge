import { StyleSheet } from "react-native";

export default StyleSheet.create({
  emptyContainer: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },
  emptyText: {
    textAlign: "center",
    marginBottom: 10,
  },
});
