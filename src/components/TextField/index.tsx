import React, { FC } from "react";
import { Text, View } from "react-native";

const TextField: FC = () => {
  return (
    <View>
      <Text>TextField</Text>
    </View>
  );
};

export default TextField;
